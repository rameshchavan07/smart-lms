import { beforeAll, afterAll } from 'vitest';
// Add any global setup like spinning up an in-memory DB or resetting DB state here.

beforeAll(() => {
  // console.log('Global setup before tests run');
});

afterAll(() => {
  // console.log('Global teardown after tests run');
});
